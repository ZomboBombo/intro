mixin _basemodal(props = {})
  - const { id } = props

  .basemodal(data-modal="window" id=id)
    button.basemodal__close-trigger(
      type="button"
      aria-label="Close modal window"
      data-modal="close-trigger"
    ) Close

    block
